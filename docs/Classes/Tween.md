<!-- github_url: hide -->

<!-- DO NOT EDIT THIS FILE!!! -->
<!-- Generated automatically from Redot engine sources. -->
<!-- Generator: https://github.com/Redot-Engine/redot-engine/tree/master/doc/tools/make_md.py. -->
<!-- XML source: https://github.com/Redot-Engine/redot-engine/tree/master/doc/classes/Tween.xml. -->

<a id="Tween"></a>

# Tween

**Inherits:** [RefCounted](RefCounted.md) **\<** [Object](Object.md)

Lightweight object used for general-purpose animation via script, using [Tweener](Tweener.md)\\ s.

<!-- classref-introduction-group -->

## Description

Tweens are mostly useful for animations requiring a numerical property to be interpolated over a range of values. The name *tween* comes from *in-betweening*, an animation technique where you specify *keyframes* and the computer interpolates the frames that appear between them. Animating something with a **Tween** is called tweening.

\ **Tween** is more suited than [AnimationPlayer](AnimationPlayer.md) for animations where you don't know the final values in advance. For example, interpolating a dynamically-chosen camera zoom value is best done with a **Tween**; it would be difficult to do the same thing with an [AnimationPlayer](AnimationPlayer.md) node. Tweens are also more light-weight than [AnimationPlayer](AnimationPlayer.md), so they are very much suited for simple animations or general tasks that don't require visual tweaking provided by the editor. They can be used in a "fire-and-forget" manner for some logic that normally would be done by code. You can e.g. make something shoot periodically by using a looped [CallbackTweener](CallbackTweener.md) with a delay.

A **Tween** can be created by using either [SceneTree.create_tween()](SceneTree.md#SceneTree_method_create_tween) or [Node.create_tween()](Node.md#Node_method_create_tween). **Tween**\\ s created manually (i.e. by using ``Tween.new()``) are invalid and can't be used for tweening values.

A tween animation is created by adding [Tweener](Tweener.md)\\ s to the **Tween** object, using [tween_property()](Tween.md#Tween_method_tween_property), [tween_interval()](Tween.md#Tween_method_tween_interval), [tween_callback()](Tween.md#Tween_method_tween_callback) or [tween_method()](Tween.md#Tween_method_tween_method):




```gdscript
    var tween = get_tree().create_tween()
    tween.tween_property($Sprite, "modulate", Color.RED, 1.0)
    tween.tween_property($Sprite, "scale", Vector2(), 1.0)
    tween.tween_callback($Sprite.queue_free)
```


```csharp
    Tween tween = GetTree().CreateTween();
    tween.TweenProperty(GetNode("Sprite"), "modulate", Colors.Red, 1.0f);
    tween.TweenProperty(GetNode("Sprite"), "scale", Vector2.Zero, 1.0f);
    tween.TweenCallback(Callable.From(GetNode("Sprite").QueueFree));
```




This sequence will make the ``$Sprite`` node turn red, then shrink, before finally calling [Node.queue_free()](Node.md#Node_method_queue_free) to free the sprite. [Tweener](Tweener.md)\\ s are executed one after another by default. This behavior can be changed using [parallel()](Tween.md#Tween_method_parallel) and [set_parallel()](Tween.md#Tween_method_set_parallel).

When a [Tweener](Tweener.md) is created with one of the ``tween_*`` methods, a chained method call can be used to tweak the properties of this [Tweener](Tweener.md). For example, if you want to set a different transition type in the above example, you can use [set_trans()](Tween.md#Tween_method_set_trans):




```gdscript
    var tween = get_tree().create_tween()
    tween.tween_property($Sprite, "modulate", Color.RED, 1.0).set_trans(Tween.TRANS_SINE)
    tween.tween_property($Sprite, "scale", Vector2(), 1.0).set_trans(Tween.TRANS_BOUNCE)
    tween.tween_callback($Sprite.queue_free)
```


```csharp
    Tween tween = GetTree().CreateTween();
    tween.TweenProperty(GetNode("Sprite"), "modulate", Colors.Red, 1.0f).SetTrans(Tween.TransitionType.Sine);
    tween.TweenProperty(GetNode("Sprite"), "scale", Vector2.Zero, 1.0f).SetTrans(Tween.TransitionType.Bounce);
    tween.TweenCallback(Callable.From(GetNode("Sprite").QueueFree));
```




Most of the **Tween** methods can be chained this way too. In the following example the **Tween** is bound to the running script's node and a default transition is set for its [Tweener](Tweener.md)\\ s:




```gdscript
    var tween = get_tree().create_tween().bind_node(self).set_trans(Tween.TRANS_ELASTIC)
    tween.tween_property($Sprite, "modulate", Color.RED, 1.0)
    tween.tween_property($Sprite, "scale", Vector2(), 1.0)
    tween.tween_callback($Sprite.queue_free)
```


```csharp
    var tween = GetTree().CreateTween().BindNode(this).SetTrans(Tween.TransitionType.Elastic);
    tween.TweenProperty(GetNode("Sprite"), "modulate", Colors.Red, 1.0f);
    tween.TweenProperty(GetNode("Sprite"), "scale", Vector2.Zero, 1.0f);
    tween.TweenCallback(Callable.From(GetNode("Sprite").QueueFree));
```




Another interesting use for **Tween**\\ s is animating arbitrary sets of objects:




```gdscript
    var tween = create_tween()
    for sprite in get_children():
        tween.tween_property(sprite, "position", Vector2(0, 0), 1.0)
```


```csharp
    Tween tween = CreateTween();
    foreach (Node sprite in GetChildren())
        tween.TweenProperty(sprite, "position", Vector2.Zero, 1.0f);
```




In the example above, all children of a node are moved one after another to position ``(0, 0)``.

You should avoid using more than one **Tween** per object's property. If two or more tweens animate one property at the same time, the last one created will take priority and assign the final value. If you want to interrupt and restart an animation, consider assigning the **Tween** to a variable:




```gdscript
    var tween
    func animate():
        if tween:
            tween.kill() # Abort the previous animation.
        tween = create_tween()
```


```csharp
    private Tween _tween;

    public void Animate()
    {
        if (_tween != null)
            _tween.Kill(); // Abort the previous animation
        _tween = CreateTween();
    }
```




Some [Tweener](Tweener.md)\\ s use transitions and eases. The first accepts a [TransitionType](Tween.md#enum_Tween_TransitionType) constant, and refers to the way the timing of the animation is handled (see [easings.net](https://easings.net/) for some examples). The second accepts an [EaseType](Tween.md#enum_Tween_EaseType) constant, and controls where the ``trans_type`` is applied to the interpolation (in the beginning, the end, or both). If you don't know which transition and easing to pick, you can try different [TransitionType](Tween.md#enum_Tween_TransitionType) constants with [EASE_IN_OUT](Tween.md#Tween_constant_EASE_IN_OUT), and use the one that looks best.

\ [Tween easing and transition types cheatsheet](https://raw.githubusercontent.com/Redot-Engine/redot-docs/master/img/tween_cheatsheet.webp)\ 

\ **Note:** Tweens are not designed to be reused and trying to do so results in an undefined behavior. Create a new Tween for each animation and every time you replay an animation from start. Keep in mind that Tweens start immediately, so only create a Tween when you want to start animating.

\ **Note:** The tween is processed after all of the nodes in the current frame, i.e. node's [Node._process()](Node.md#Node_private_method__process) method would be called before the tween (or [Node._physics_process()](Node.md#Node_private_method__physics_process) depending on the value passed to [set_process_mode()](Tween.md#Tween_method_set_process_mode)).

<!-- classref-reftable-group -->

## Methods

<table>
  <thead>
    <tr>
      <th>[Tween](Tween.md)</th>
      <th>[bind_node](#Tween_method_bind_node)\ (\ node\: [Node](Node.md)\ )</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[chain](#Tween_method_chain)\ (\ )</td>
    </tr>
    <tr>
      <td>[bool](bool.md)</td>
      <td>[custom_step](#Tween_method_custom_step)\ (\ delta\: [float](float.md)\ )</td>
    </tr>
    <tr>
      <td>[int](int.md)</td>
      <td>[get_loops_left](#Tween_method_get_loops_left)\ (\ ) <span class="const">const</span></td>
    </tr>
    <tr>
      <td>[float](float.md)</td>
      <td>[get_total_elapsed_time](#Tween_method_get_total_elapsed_time)\ (\ ) <span class="const">const</span></td>
    </tr>
    <tr>
      <td>[Variant](Variant.md)</td>
      <td>[interpolate_value](#Tween_method_interpolate_value)\ (\ initial_value\: [Variant](Variant.md), delta_value\: [Variant](Variant.md), elapsed_time\: [float](float.md), duration\: [float](float.md), trans_type\: [TransitionType](Tween.md#enum_Tween_TransitionType), ease_type\: [EaseType](Tween.md#enum_Tween_EaseType)\ ) <span class="static">static</span></td>
    </tr>
    <tr>
      <td>[bool](bool.md)</td>
      <td>[is_running](#Tween_method_is_running)\ (\ )</td>
    </tr>
    <tr>
      <td>[bool](bool.md)</td>
      <td>[is_valid](#Tween_method_is_valid)\ (\ )</td>
    </tr>
    <tr>
      <td>`void`</td>
      <td>[kill](#Tween_method_kill)\ (\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[parallel](#Tween_method_parallel)\ (\ )</td>
    </tr>
    <tr>
      <td>`void`</td>
      <td>[pause](#Tween_method_pause)\ (\ )</td>
    </tr>
    <tr>
      <td>`void`</td>
      <td>[play](#Tween_method_play)\ (\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[set_ease](#Tween_method_set_ease)\ (\ ease\: [EaseType](Tween.md#enum_Tween_EaseType)\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[set_ignore_time_scale](#Tween_method_set_ignore_time_scale)\ (\ ignore\: [bool](bool.md) = true\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[set_loops](#Tween_method_set_loops)\ (\ loops\: [int](int.md) = 0\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[set_parallel](#Tween_method_set_parallel)\ (\ parallel\: [bool](bool.md) = true\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[set_pause_mode](#Tween_method_set_pause_mode)\ (\ mode\: [TweenPauseMode](Tween.md#enum_Tween_TweenPauseMode)\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[set_process_mode](#Tween_method_set_process_mode)\ (\ mode\: [TweenProcessMode](Tween.md#enum_Tween_TweenProcessMode)\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[set_speed_scale](#Tween_method_set_speed_scale)\ (\ speed\: [float](float.md)\ )</td>
    </tr>
    <tr>
      <td>[Tween](Tween.md)</td>
      <td>[set_trans](#Tween_method_set_trans)\ (\ trans\: [TransitionType](Tween.md#enum_Tween_TransitionType)\ )</td>
    </tr>
    <tr>
      <td>`void`</td>
      <td>[stop](#Tween_method_stop)\ (\ )</td>
    </tr>
    <tr>
      <td>[CallbackTweener](CallbackTweener.md)</td>
      <td>[tween_callback](#Tween_method_tween_callback)\ (\ callback\: [Callable](Callable.md)\ )</td>
    </tr>
    <tr>
      <td>[IntervalTweener](IntervalTweener.md)</td>
      <td>[tween_interval](#Tween_method_tween_interval)\ (\ time\: [float](float.md)\ )</td>
    </tr>
    <tr>
      <td>[MethodTweener](MethodTweener.md)</td>
      <td>[tween_method](#Tween_method_tween_method)\ (\ method\: [Callable](Callable.md), from\: [Variant](Variant.md), to\: [Variant](Variant.md), duration\: [float](float.md)\ )</td>
    </tr>
    <tr>
      <td>[PropertyTweener](PropertyTweener.md)</td>
      <td>[tween_property](#Tween_method_tween_property)\ (\ object\: [Object](Object.md), property\: [NodePath](NodePath.md), final_val\: [Variant](Variant.md), duration\: [float](float.md)\ )</td>
    </tr>
    <tr>
      <td>[SubtweenTweener](SubtweenTweener.md)</td>
      <td>[tween_subtween](#Tween_method_tween_subtween)\ (\ subtween\: [Tween](Tween.md)\ )</td>
    </tr>
  </tbody>
</table>

<hr class="classref-section-separator"/>

<!-- classref-descriptions-group -->

## Signals

<a id="Tween_signal_finished"></a>

<!-- classref-signal -->

**finished**\ (\ ) [🔗](#Tween_signal_finished)

Emitted when the **Tween** has finished all tweening. Never emitted when the **Tween** is set to infinite looping (see [set_loops()](Tween.md#Tween_method_set_loops)).

<hr class="classref-item-separator"/>

<a id="Tween_signal_loop_finished"></a>

<!-- classref-signal -->

**loop_finished**\ (\ loop_count\: [int](int.md)\ ) [🔗](#Tween_signal_loop_finished)

Emitted when a full loop is complete (see [set_loops()](Tween.md#Tween_method_set_loops)), providing the loop index. This signal is not emitted after the final loop, use [finished](Tween.md#Tween_signal_finished) instead for this case.

<hr class="classref-item-separator"/>

<a id="Tween_signal_step_finished"></a>

<!-- classref-signal -->

**step_finished**\ (\ idx\: [int](int.md)\ ) [🔗](#Tween_signal_step_finished)

Emitted when one step of the **Tween** is complete, providing the step index. One step is either a single [Tweener](Tweener.md) or a group of [Tweener](Tweener.md)\\ s running in parallel.

<hr class="classref-section-separator"/>

<!-- classref-descriptions-group -->

## Enumerations

<a id="enum_Tween_TweenProcessMode"></a>

<!-- classref-enumeration -->

enum **TweenProcessMode**: [🔗](#enum_Tween_TweenProcessMode)

<a id="Tween_constant_TWEEN_PROCESS_PHYSICS"></a>

<!-- classref-enumeration-constant -->

[TweenProcessMode](Tween.md#enum_Tween_TweenProcessMode) **TWEEN_PROCESS_PHYSICS** = `0`

The **Tween** updates after each physics frame (see [Node._physics_process()](Node.md#Node_private_method__physics_process)).<a id="Tween_constant_TWEEN_PROCESS_IDLE"></a>

<!-- classref-enumeration-constant -->

[TweenProcessMode](Tween.md#enum_Tween_TweenProcessMode) **TWEEN_PROCESS_IDLE** = `1`

The **Tween** updates after each process frame (see [Node._process()](Node.md#Node_private_method__process)).<hr class="classref-item-separator"/>

<a id="enum_Tween_TweenPauseMode"></a>

<!-- classref-enumeration -->

enum **TweenPauseMode**: [🔗](#enum_Tween_TweenPauseMode)

<a id="Tween_constant_TWEEN_PAUSE_BOUND"></a>

<!-- classref-enumeration-constant -->

[TweenPauseMode](Tween.md#enum_Tween_TweenPauseMode) **TWEEN_PAUSE_BOUND** = `0`

If the **Tween** has a bound node, it will process when that node can process (see [Node.process_mode](Node.md#Node_property_process_mode)). Otherwise it's the same as [TWEEN_PAUSE_STOP](Tween.md#Tween_constant_TWEEN_PAUSE_STOP).<a id="Tween_constant_TWEEN_PAUSE_STOP"></a>

<!-- classref-enumeration-constant -->

[TweenPauseMode](Tween.md#enum_Tween_TweenPauseMode) **TWEEN_PAUSE_STOP** = `1`

If [SceneTree](SceneTree.md) is paused, the **Tween** will also pause.<a id="Tween_constant_TWEEN_PAUSE_PROCESS"></a>

<!-- classref-enumeration-constant -->

[TweenPauseMode](Tween.md#enum_Tween_TweenPauseMode) **TWEEN_PAUSE_PROCESS** = `2`

The **Tween** will process regardless of whether [SceneTree](SceneTree.md) is paused.<hr class="classref-item-separator"/>

<a id="enum_Tween_TransitionType"></a>

<!-- classref-enumeration -->

enum **TransitionType**: [🔗](#enum_Tween_TransitionType)

<a id="Tween_constant_TRANS_LINEAR"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_LINEAR** = `0`

The animation is interpolated linearly.<a id="Tween_constant_TRANS_SINE"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_SINE** = `1`

The animation is interpolated using a sine function.<a id="Tween_constant_TRANS_QUINT"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_QUINT** = `2`

The animation is interpolated with a quintic (to the power of 5) function.<a id="Tween_constant_TRANS_QUART"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_QUART** = `3`

The animation is interpolated with a quartic (to the power of 4) function.<a id="Tween_constant_TRANS_QUAD"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_QUAD** = `4`

The animation is interpolated with a quadratic (to the power of 2) function.<a id="Tween_constant_TRANS_EXPO"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_EXPO** = `5`

The animation is interpolated with an exponential (to the power of x) function.<a id="Tween_constant_TRANS_ELASTIC"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_ELASTIC** = `6`

The animation is interpolated with elasticity, wiggling around the edges.<a id="Tween_constant_TRANS_CUBIC"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_CUBIC** = `7`

The animation is interpolated with a cubic (to the power of 3) function.<a id="Tween_constant_TRANS_CIRC"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_CIRC** = `8`

The animation is interpolated with a function using square roots.<a id="Tween_constant_TRANS_BOUNCE"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_BOUNCE** = `9`

The animation is interpolated by bouncing at the end.<a id="Tween_constant_TRANS_BACK"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_BACK** = `10`

The animation is interpolated backing out at ends.<a id="Tween_constant_TRANS_SPRING"></a>

<!-- classref-enumeration-constant -->

[TransitionType](Tween.md#enum_Tween_TransitionType) **TRANS_SPRING** = `11`

The animation is interpolated like a spring towards the end.<hr class="classref-item-separator"/>

<a id="enum_Tween_EaseType"></a>

<!-- classref-enumeration -->

enum **EaseType**: [🔗](#enum_Tween_EaseType)

<a id="Tween_constant_EASE_IN"></a>

<!-- classref-enumeration-constant -->

[EaseType](Tween.md#enum_Tween_EaseType) **EASE_IN** = `0`

The interpolation starts slowly and speeds up towards the end.<a id="Tween_constant_EASE_OUT"></a>

<!-- classref-enumeration-constant -->

[EaseType](Tween.md#enum_Tween_EaseType) **EASE_OUT** = `1`

The interpolation starts quickly and slows down towards the end.<a id="Tween_constant_EASE_IN_OUT"></a>

<!-- classref-enumeration-constant -->

[EaseType](Tween.md#enum_Tween_EaseType) **EASE_IN_OUT** = `2`

A combination of [EASE_IN](Tween.md#Tween_constant_EASE_IN) and [EASE_OUT](Tween.md#Tween_constant_EASE_OUT). The interpolation is slowest at both ends.<a id="Tween_constant_EASE_OUT_IN"></a>

<!-- classref-enumeration-constant -->

[EaseType](Tween.md#enum_Tween_EaseType) **EASE_OUT_IN** = `3`

A combination of [EASE_IN](Tween.md#Tween_constant_EASE_IN) and [EASE_OUT](Tween.md#Tween_constant_EASE_OUT). The interpolation is fastest at both ends.<hr class="classref-section-separator"/>

<!-- classref-descriptions-group -->

## Method Descriptions

<a id="Tween_method_bind_node"></a>

<!-- classref-method -->

[Tween](Tween.md) **bind_node**\ (\ node\: [Node](Node.md)\ ) [🔗](#Tween_method_bind_node)

Binds this **Tween** with the given `node`. **Tween**\\ s are processed directly by the [SceneTree](SceneTree.md), so they run independently of the animated nodes. When you bind a [Node](Node.md) with the **Tween**, the **Tween** will halt the animation when the object is not inside tree and the **Tween** will be automatically killed when the bound object is freed. Also [TWEEN_PAUSE_BOUND](Tween.md#Tween_constant_TWEEN_PAUSE_BOUND) will make the pausing behavior dependent on the bound node.

For a shorter way to create and bind a **Tween**, you can use [Node.create_tween()](Node.md#Node_method_create_tween).

<hr class="classref-item-separator"/>

<a id="Tween_method_chain"></a>

<!-- classref-method -->

[Tween](Tween.md) **chain**\ (\ ) [🔗](#Tween_method_chain)

Used to chain two [Tweener](Tweener.md)\\ s after [set_parallel()](Tween.md#Tween_method_set_parallel) is called with ``true``.




```gdscript
    var tween = create_tween().set_parallel(true)
    tween.tween_property(...)
    tween.tween_property(...) # Will run parallelly with above.
    tween.chain().tween_property(...) # Will run after two above are finished.
```


```csharp
    Tween tween = CreateTween().SetParallel(true);
    tween.TweenProperty(...);
    tween.TweenProperty(...); // Will run parallelly with above.
    tween.Chain().TweenProperty(...); // Will run after two above are finished.
```




<hr class="classref-item-separator"/>

<a id="Tween_method_custom_step"></a>

<!-- classref-method -->

[bool](bool.md) **custom_step**\ (\ delta\: [float](float.md)\ ) [🔗](#Tween_method_custom_step)

Processes the **Tween** by the given `delta` value, in seconds. This is mostly useful for manual control when the **Tween** is paused. It can also be used to end the **Tween** animation immediately, by setting `delta` longer than the whole duration of the **Tween** animation.

Returns ``true`` if the **Tween** still has [Tweener](Tweener.md)\\ s that haven't finished.

<hr class="classref-item-separator"/>

<a id="Tween_method_get_loops_left"></a>

<!-- classref-method -->

[int](int.md) **get_loops_left**\ (\ ) <span class="const">const</span> [🔗](#Tween_method_get_loops_left)

Returns the number of remaining loops for this **Tween** (see [set_loops()](Tween.md#Tween_method_set_loops)). A return value of ``-1`` indicates an infinitely looping **Tween**, and a return value of ``0`` indicates that the **Tween** has already finished.

<hr class="classref-item-separator"/>

<a id="Tween_method_get_total_elapsed_time"></a>

<!-- classref-method -->

[float](float.md) **get_total_elapsed_time**\ (\ ) <span class="const">const</span> [🔗](#Tween_method_get_total_elapsed_time)

Returns the total time in seconds the **Tween** has been animating (i.e. the time since it started, not counting pauses etc.). The time is affected by [set_speed_scale()](Tween.md#Tween_method_set_speed_scale), and [stop()](Tween.md#Tween_method_stop) will reset it to ``0``.

\ **Note:** As it results from accumulating frame deltas, the time returned after the **Tween** has finished animating will be slightly greater than the actual **Tween** duration.

<hr class="classref-item-separator"/>

<a id="Tween_method_interpolate_value"></a>

<!-- classref-method -->

[Variant](Variant.md) **interpolate_value**\ (\ initial_value\: [Variant](Variant.md), delta_value\: [Variant](Variant.md), elapsed_time\: [float](float.md), duration\: [float](float.md), trans_type\: [TransitionType](Tween.md#enum_Tween_TransitionType), ease_type\: [EaseType](Tween.md#enum_Tween_EaseType)\ ) <span class="static">static</span> [🔗](#Tween_method_interpolate_value)

This method can be used for manual interpolation of a value, when you don't want **Tween** to do animating for you. It's similar to [@GlobalScope.lerp()](@GlobalScope.md#@GlobalScope_method_lerp), but with support for custom transition and easing.

\ `initial_value` is the starting value of the interpolation.

\ `delta_value` is the change of the value in the interpolation, i.e. it's equal to ``final_value - initial_value``.

\ `elapsed_time` is the time in seconds that passed after the interpolation started and it's used to control the position of the interpolation. E.g. when it's equal to half of the `duration`, the interpolated value will be halfway between initial and final values. This value can also be greater than `duration` or lower than 0, which will extrapolate the value.

\ `duration` is the total time of the interpolation.

\ **Note:** If `duration` is equal to ``0``, the method will always return the final value, regardless of `elapsed_time` provided.

<hr class="classref-item-separator"/>

<a id="Tween_method_is_running"></a>

<!-- classref-method -->

[bool](bool.md) **is_running**\ (\ ) [🔗](#Tween_method_is_running)

Returns whether the **Tween** is currently running, i.e. it wasn't paused and it's not finished.

<hr class="classref-item-separator"/>

<a id="Tween_method_is_valid"></a>

<!-- classref-method -->

[bool](bool.md) **is_valid**\ (\ ) [🔗](#Tween_method_is_valid)

Returns whether the **Tween** is valid. A valid **Tween** is a **Tween** contained by the scene tree (i.e. the array from [SceneTree.get_processed_tweens()](SceneTree.md#SceneTree_method_get_processed_tweens) will contain this **Tween**). A **Tween** might become invalid when it has finished tweening, is killed, or when created with ``Tween.new()``. Invalid **Tween**\\ s can't have [Tweener](Tweener.md)\\ s appended.

<hr class="classref-item-separator"/>

<a id="Tween_method_kill"></a>

<!-- classref-method -->

`void` **kill**\ (\ ) [🔗](#Tween_method_kill)

Aborts all tweening operations and invalidates the **Tween**.

<hr class="classref-item-separator"/>

<a id="Tween_method_parallel"></a>

<!-- classref-method -->

[Tween](Tween.md) **parallel**\ (\ ) [🔗](#Tween_method_parallel)

Makes the next [Tweener](Tweener.md) run parallelly to the previous one.




```gdscript
    var tween = create_tween()
    tween.tween_property(...)
    tween.parallel().tween_property(...)
    tween.parallel().tween_property(...)
```


```csharp
    Tween tween = CreateTween();
    tween.TweenProperty(...);
    tween.Parallel().TweenProperty(...);
    tween.Parallel().TweenProperty(...);
```




All [Tweener](Tweener.md)\\ s in the example will run at the same time.

You can make the **Tween** parallel by default by using [set_parallel()](Tween.md#Tween_method_set_parallel).

<hr class="classref-item-separator"/>

<a id="Tween_method_pause"></a>

<!-- classref-method -->

`void` **pause**\ (\ ) [🔗](#Tween_method_pause)

Pauses the tweening. The animation can be resumed by using [play()](Tween.md#Tween_method_play).

\ **Note:** If a Tween is paused and not bound to any node, it will exist indefinitely until manually started or invalidated. If you lose a reference to such Tween, you can retrieve it using [SceneTree.get_processed_tweens()](SceneTree.md#SceneTree_method_get_processed_tweens).

<hr class="classref-item-separator"/>

<a id="Tween_method_play"></a>

<!-- classref-method -->

`void` **play**\ (\ ) [🔗](#Tween_method_play)

Resumes a paused or stopped **Tween**.

<hr class="classref-item-separator"/>

<a id="Tween_method_set_ease"></a>

<!-- classref-method -->

[Tween](Tween.md) **set_ease**\ (\ ease\: [EaseType](Tween.md#enum_Tween_EaseType)\ ) [🔗](#Tween_method_set_ease)

Sets the default ease type for [PropertyTweener](PropertyTweener.md)\\ s and [MethodTweener](MethodTweener.md)\\ s appended after this method.

Before this method is called, the default ease type is [EASE_IN_OUT](Tween.md#Tween_constant_EASE_IN_OUT).

```
    var tween = create_tween()
    tween.tween_property(self, "position", Vector2(300, 0), 0.5) # Uses EASE_IN_OUT.
    tween.set_ease(Tween.EASE_IN)
    tween.tween_property(self, "rotation_degrees", 45.0, 0.5) # Uses EASE_IN.
```


<hr class="classref-item-separator"/>

<a id="Tween_method_set_ignore_time_scale"></a>

<!-- classref-method -->

[Tween](Tween.md) **set_ignore_time_scale**\ (\ ignore\: [bool](bool.md) = true\ ) [🔗](#Tween_method_set_ignore_time_scale)

If `ignore` is ``true``, the tween will ignore [Engine.time_scale](Engine.md#Engine_property_time_scale) and update with the real, elapsed time. This affects all [Tweener](Tweener.md)\\ s and their delays. Default value is ``false``.

<hr class="classref-item-separator"/>

<a id="Tween_method_set_loops"></a>

<!-- classref-method -->

[Tween](Tween.md) **set_loops**\ (\ loops\: [int](int.md) = 0\ ) [🔗](#Tween_method_set_loops)

Sets the number of times the tweening sequence will be repeated, i.e. ``set_loops(2)`` will run the animation twice.

Calling this method without arguments will make the **Tween** run infinitely, until either it is killed with [kill()](Tween.md#Tween_method_kill), the **Tween**'s bound node is freed, or all the animated objects have been freed (which makes further animation impossible).

\ **Warning:** Make sure to always add some duration/delay when using infinite loops. To prevent the game freezing, 0-duration looped animations (e.g. a single [CallbackTweener](CallbackTweener.md) with no delay) are stopped after a small number of loops, which may produce unexpected results. If a **Tween**'s lifetime depends on some node, always use [bind_node()](Tween.md#Tween_method_bind_node).

<hr class="classref-item-separator"/>

<a id="Tween_method_set_parallel"></a>

<!-- classref-method -->

[Tween](Tween.md) **set_parallel**\ (\ parallel\: [bool](bool.md) = true\ ) [🔗](#Tween_method_set_parallel)

If `parallel` is ``true``, the [Tweener](Tweener.md)\\ s appended after this method will by default run simultaneously, as opposed to sequentially.

\ **Note:** Just like with [parallel()](Tween.md#Tween_method_parallel), the tweener added right before this method will also be part of the parallel step.

```
    tween.tween_property(self, "position", Vector2(300, 0), 0.5)
    tween.set_parallel()
    tween.tween_property(self, "modulate", Color.GREEN, 0.5) # Runs together with the position tweener.
```


<hr class="classref-item-separator"/>

<a id="Tween_method_set_pause_mode"></a>

<!-- classref-method -->

[Tween](Tween.md) **set_pause_mode**\ (\ mode\: [TweenPauseMode](Tween.md#enum_Tween_TweenPauseMode)\ ) [🔗](#Tween_method_set_pause_mode)

Determines the behavior of the **Tween** when the [SceneTree](SceneTree.md) is paused.

Default value is [TWEEN_PAUSE_BOUND](Tween.md#Tween_constant_TWEEN_PAUSE_BOUND).

<hr class="classref-item-separator"/>

<a id="Tween_method_set_process_mode"></a>

<!-- classref-method -->

[Tween](Tween.md) **set_process_mode**\ (\ mode\: [TweenProcessMode](Tween.md#enum_Tween_TweenProcessMode)\ ) [🔗](#Tween_method_set_process_mode)

Determines whether the **Tween** should run after process frames (see [Node._process()](Node.md#Node_private_method__process)) or physics frames (see [Node._physics_process()](Node.md#Node_private_method__physics_process)).

Default value is [TWEEN_PROCESS_IDLE](Tween.md#Tween_constant_TWEEN_PROCESS_IDLE).

<hr class="classref-item-separator"/>

<a id="Tween_method_set_speed_scale"></a>

<!-- classref-method -->

[Tween](Tween.md) **set_speed_scale**\ (\ speed\: [float](float.md)\ ) [🔗](#Tween_method_set_speed_scale)

Scales the speed of tweening. This affects all [Tweener](Tweener.md)\\ s and their delays.

<hr class="classref-item-separator"/>

<a id="Tween_method_set_trans"></a>

<!-- classref-method -->

[Tween](Tween.md) **set_trans**\ (\ trans\: [TransitionType](Tween.md#enum_Tween_TransitionType)\ ) [🔗](#Tween_method_set_trans)

Sets the default transition type for [PropertyTweener](PropertyTweener.md)\\ s and [MethodTweener](MethodTweener.md)\\ s appended after this method.

Before this method is called, the default transition type is [TRANS_LINEAR](Tween.md#Tween_constant_TRANS_LINEAR).

```
    var tween = create_tween()
    tween.tween_property(self, "position", Vector2(300, 0), 0.5) # Uses TRANS_LINEAR.
    tween.set_trans(Tween.TRANS_SINE)
    tween.tween_property(self, "rotation_degrees", 45.0, 0.5) # Uses TRANS_SINE.
```


<hr class="classref-item-separator"/>

<a id="Tween_method_stop"></a>

<!-- classref-method -->

`void` **stop**\ (\ ) [🔗](#Tween_method_stop)

Stops the tweening and resets the **Tween** to its initial state. This will not remove any appended [Tweener](Tweener.md)\\ s.

\ **Note:** This does *not* reset targets of [PropertyTweener](PropertyTweener.md)\\ s to their values when the **Tween** first started.

```
    var tween = create_tween()

    # Will move from 0 to 500 over 1 second.
    position.x = 0.0
    tween.tween_property(self, "position:x", 500, 1.0)

    # Will be at (about) 250 when the timer finishes.
    await get_tree().create_timer(0.5).timeout

    # Will now move from (about) 250 to 500 over 1 second,
    # thus at half the speed as before.
    tween.stop()
    tween.play()
```


\ **Note:** If a Tween is stopped and not bound to any node, it will exist indefinitely until manually started or invalidated. If you lose a reference to such Tween, you can retrieve it using [SceneTree.get_processed_tweens()](SceneTree.md#SceneTree_method_get_processed_tweens).

<hr class="classref-item-separator"/>

<a id="Tween_method_tween_callback"></a>

<!-- classref-method -->

[CallbackTweener](CallbackTweener.md) **tween_callback**\ (\ callback\: [Callable](Callable.md)\ ) [🔗](#Tween_method_tween_callback)

Creates and appends a [CallbackTweener](CallbackTweener.md). This method can be used to call an arbitrary method in any object. Use [Callable.bind()](Callable.md#Callable_method_bind) to bind additional arguments for the call.

\ **Example:** Object that keeps shooting every 1 second:




```gdscript
    var tween = get_tree().create_tween().set_loops()
    tween.tween_callback(shoot).set_delay(1.0)
```


```csharp
    Tween tween = GetTree().CreateTween().SetLoops();
    tween.TweenCallback(Callable.From(Shoot)).SetDelay(1.0f);
```




\ **Example:** Turning a sprite red and then blue, with 2 second delay:




```gdscript
    var tween = get_tree().create_tween()
    tween.tween_callback($Sprite.set_modulate.bind(Color.RED)).set_delay(2)
    tween.tween_callback($Sprite.set_modulate.bind(Color.BLUE)).set_delay(2)
```


```csharp
    Tween tween = GetTree().CreateTween();
    Sprite2D sprite = GetNode&lt;Sprite2D&gt;("Sprite");
    tween.TweenCallback(Callable.From(() =&gt; sprite.Modulate = Colors.Red)).SetDelay(2.0f);
    tween.TweenCallback(Callable.From(() =&gt; sprite.Modulate = Colors.Blue)).SetDelay(2.0f);
```




<hr class="classref-item-separator"/>

<a id="Tween_method_tween_interval"></a>

<!-- classref-method -->

[IntervalTweener](IntervalTweener.md) **tween_interval**\ (\ time\: [float](float.md)\ ) [🔗](#Tween_method_tween_interval)

Creates and appends an [IntervalTweener](IntervalTweener.md). This method can be used to create delays in the tween animation, as an alternative to using the delay in other [Tweener](Tweener.md)\\ s, or when there's no animation (in which case the **Tween** acts as a timer). `time` is the length of the interval, in seconds.

\ **Example:** Creating an interval in code execution:




```gdscript
    # ... some code
    await create_tween().tween_interval(2).finished
    # ... more code
```


```csharp
    // ... some code
    await ToSignal(CreateTween().TweenInterval(2.0f), Tween.SignalName.Finished);
    // ... more code
```




\ **Example:** Creating an object that moves back and forth and jumps every few seconds:




```gdscript
    var tween = create_tween().set_loops()
    tween.tween_property($Sprite, "position:x", 200.0, 1.0).as_relative()
    tween.tween_callback(jump)
    tween.tween_interval(2)
    tween.tween_property($Sprite, "position:x", -200.0, 1.0).as_relative()
    tween.tween_callback(jump)
    tween.tween_interval(2)
```


```csharp
    Tween tween = CreateTween().SetLoops();
    tween.TweenProperty(GetNode("Sprite"), "position:x", 200.0f, 1.0f).AsRelative();
    tween.TweenCallback(Callable.From(Jump));
    tween.TweenInterval(2.0f);
    tween.TweenProperty(GetNode("Sprite"), "position:x", -200.0f, 1.0f).AsRelative();
    tween.TweenCallback(Callable.From(Jump));
    tween.TweenInterval(2.0f);
```




<hr class="classref-item-separator"/>

<a id="Tween_method_tween_method"></a>

<!-- classref-method -->

[MethodTweener](MethodTweener.md) **tween_method**\ (\ method\: [Callable](Callable.md), from\: [Variant](Variant.md), to\: [Variant](Variant.md), duration\: [float](float.md)\ ) [🔗](#Tween_method_tween_method)

Creates and appends a [MethodTweener](MethodTweener.md). This method is similar to a combination of [tween_callback()](Tween.md#Tween_method_tween_callback) and [tween_property()](Tween.md#Tween_method_tween_property). It calls a method over time with a tweened value provided as an argument. The value is tweened between `from` and `to` over the time specified by `duration`, in seconds. Use [Callable.bind()](Callable.md#Callable_method_bind) to bind additional arguments for the call. You can use [MethodTweener.set_ease()](MethodTweener.md#MethodTweener_method_set_ease) and [MethodTweener.set_trans()](MethodTweener.md#MethodTweener_method_set_trans) to tweak the easing and transition of the value or [MethodTweener.set_delay()](MethodTweener.md#MethodTweener_method_set_delay) to delay the tweening.

\ **Example:** Making a 3D object look from one point to another point:




```gdscript
    var tween = create_tween()
    tween.tween_method(look_at.bind(Vector3.UP), Vector3(-1, 0, -1), Vector3(1, 0, -1), 1.0) # The look_at() method takes up vector as second argument.
```


```csharp
    Tween tween = CreateTween();
    tween.TweenMethod(Callable.From((Vector3 target) =&gt; LookAt(target, Vector3.Up)), new Vector3(-1.0f, 0.0f, -1.0f), new Vector3(1.0f, 0.0f, -1.0f), 1.0f); // Use lambdas to bind additional arguments for the call.
```




\ **Example:** Setting the text of a [Label](Label.md), using an intermediate method and after a delay:




```gdscript
    func _ready():
        var tween = create_tween()
        tween.tween_method(set_label_text, 0, 10, 1.0).set_delay(1.0)

    func set_label_text(value: int):
        $Label.text = "Counting " + str(value)
```


```csharp
    public override void _Ready()
    {
        base._Ready();

        Tween tween = CreateTween();
        tween.TweenMethod(Callable.From&lt;int&gt;(SetLabelText), 0.0f, 10.0f, 1.0f).SetDelay(1.0f);
    }

    private void SetLabelText(int value)
    {
        GetNode&lt;Label&gt;("Label").Text = $"Counting {value}";
    }
```




<hr class="classref-item-separator"/>

<a id="Tween_method_tween_property"></a>

<!-- classref-method -->

[PropertyTweener](PropertyTweener.md) **tween_property**\ (\ object\: [Object](Object.md), property\: [NodePath](NodePath.md), final_val\: [Variant](Variant.md), duration\: [float](float.md)\ ) [🔗](#Tween_method_tween_property)

Creates and appends a [PropertyTweener](PropertyTweener.md). This method tweens a `property` of an `object` between an initial value and `final_val` in a span of time equal to `duration`, in seconds. The initial value by default is the property's value at the time the tweening of the [PropertyTweener](PropertyTweener.md) starts.




```gdscript
    var tween = create_tween()
    tween.tween_property($Sprite, "position", Vector2(100, 200), 1.0)
    tween.tween_property($Sprite, "position", Vector2(200, 300), 1.0)
```


```csharp
    Tween tween = CreateTween();
    tween.TweenProperty(GetNode("Sprite"), "position", new Vector2(100.0f, 200.0f), 1.0f);
    tween.TweenProperty(GetNode("Sprite"), "position", new Vector2(200.0f, 300.0f), 1.0f);
```




will move the sprite to position (100, 200) and then to (200, 300). If you use [PropertyTweener.from()](PropertyTweener.md#PropertyTweener_method_from) or [PropertyTweener.from_current()](PropertyTweener.md#PropertyTweener_method_from_current), the starting position will be overwritten by the given value instead. See other methods in [PropertyTweener](PropertyTweener.md) to see how the tweening can be tweaked further.

\ **Note:** You can find the correct property name by hovering over the property in the Inspector. You can also provide the components of a property directly by using ``"property:component"`` (eg. ``position:x``), where it would only apply to that particular component.

\ **Example:** Moving an object twice from the same position, with different transition types:




```gdscript
    var tween = create_tween()
    tween.tween_property($Sprite, "position", Vector2.RIGHT * 300, 1.0).as_relative().set_trans(Tween.TRANS_SINE)
    tween.tween_property($Sprite, "position", Vector2.RIGHT * 300, 1.0).as_relative().from_current().set_trans(Tween.TRANS_EXPO)
```


```csharp
    Tween tween = CreateTween();
    tween.TweenProperty(GetNode("Sprite"), "position", Vector2.Right * 300.0f, 1.0f).AsRelative().SetTrans(Tween.TransitionType.Sine);
    tween.TweenProperty(GetNode("Sprite"), "position", Vector2.Right * 300.0f, 1.0f).AsRelative().FromCurrent().SetTrans(Tween.TransitionType.Expo);
```




<hr class="classref-item-separator"/>

<a id="Tween_method_tween_subtween"></a>

<!-- classref-method -->

[SubtweenTweener](SubtweenTweener.md) **tween_subtween**\ (\ subtween\: [Tween](Tween.md)\ ) [🔗](#Tween_method_tween_subtween)

Creates and appends a [SubtweenTweener](SubtweenTweener.md). This method can be used to nest `subtween` within this **Tween**, allowing for the creation of more complex and composable sequences.

```
    # Subtween will rotate the object.
    var subtween = create_tween()
    subtween.tween_property(self, "rotation_degrees", 45.0, 1.0)
    subtween.tween_property(self, "rotation_degrees", 0.0, 1.0)

    # Parent tween will execute the subtween as one of its steps.
    var tween = create_tween()
    tween.tween_property(self, "position:x", 500, 3.0)
    tween.tween_subtween(subtween)
    tween.tween_property(self, "position:x", 300, 2.0)
```


\ **Note:** The methods [pause()](Tween.md#Tween_method_pause), [stop()](Tween.md#Tween_method_stop), and [set_loops()](Tween.md#Tween_method_set_loops) can cause the parent **Tween** to get stuck on the subtween step; see the documentation for those methods for more information.

\ **Note:** The pause and process modes set by [set_pause_mode()](Tween.md#Tween_method_set_pause_mode) and [set_process_mode()](Tween.md#Tween_method_set_process_mode) on `subtween` will be overridden by the parent **Tween**'s settings.


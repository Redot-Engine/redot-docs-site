<!-- github_url: hide -->

<!-- DO NOT EDIT THIS FILE!!! -->
<!-- Generated automatically from Redot engine sources. -->
<!-- Generator: https://github.com/Redot-Engine/redot-engine/tree/master/doc/tools/make_md.py. -->
<!-- XML source: https://github.com/Redot-Engine/redot-engine/tree/master/doc/classes/RandomNumberGenerator.xml. -->

<a id="RandomNumberGenerator"></a>

# RandomNumberGenerator

**Inherits:** [RefCounted](RefCounted.md) **\<** [Object](Object.md)

Provides methods for generating pseudo-random numbers.

<!-- classref-introduction-group -->

## Description

RandomNumberGenerator is a class for generating pseudo-random numbers. It currently uses [PCG32](https://www.pcg-random.org/).

\ **Note:** The underlying algorithm is an implementation detail and should not be depended upon.

To generate a random float number (within a given range) based on a time-dependent seed:

```
    var rng = RandomNumberGenerator.new()
    func _ready():
        var my_random_number = rng.randf_range(-10.0, 10.0)
```


<!-- classref-introduction-group -->

## Tutorials

- [Random number generation](../tutorials/math/random_number_generation.md)

<!-- classref-reftable-group -->

## Properties

<table>
  <thead>
    <tr>
      <th>[int](int.md)</th>
      <th>[seed](#RandomNumberGenerator_property_seed)</th>
      <th>`0`</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>[int](int.md)</td>
      <td>[state](#RandomNumberGenerator_property_state)</td>
      <td>`0`</td>
    </tr>
  </tbody>
</table>

<!-- classref-reftable-group -->

## Methods

<table>
  <thead>
    <tr>
      <th>[int](int.md)</th>
      <th>[rand_weighted](#RandomNumberGenerator_method_rand_weighted)\ (\ weights\: [PackedFloat32Array](PackedFloat32Array.md)\ )</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>[float](float.md)</td>
      <td>[randf](#RandomNumberGenerator_method_randf)\ (\ )</td>
    </tr>
    <tr>
      <td>[float](float.md)</td>
      <td>[randf_range](#RandomNumberGenerator_method_randf_range)\ (\ from\: [float](float.md), to\: [float](float.md)\ )</td>
    </tr>
    <tr>
      <td>[float](float.md)</td>
      <td>[randfn](#RandomNumberGenerator_method_randfn)\ (\ mean\: [float](float.md) = 0.0, deviation\: [float](float.md) = 1.0\ )</td>
    </tr>
    <tr>
      <td>[int](int.md)</td>
      <td>[randi](#RandomNumberGenerator_method_randi)\ (\ )</td>
    </tr>
    <tr>
      <td>[int](int.md)</td>
      <td>[randi_range](#RandomNumberGenerator_method_randi_range)\ (\ from\: [int](int.md), to\: [int](int.md)\ )</td>
    </tr>
    <tr>
      <td>`void`</td>
      <td>[randomize](#RandomNumberGenerator_method_randomize)\ (\ )</td>
    </tr>
  </tbody>
</table>

<hr class="classref-section-separator"/>

<!-- classref-descriptions-group -->

## Property Descriptions

<a id="RandomNumberGenerator_property_seed"></a>

<!-- classref-property -->

[int](int.md) **seed** = `0` [🔗](#RandomNumberGenerator_property_seed)

<!-- classref-property-setget -->

- `void` **set_seed**\ (\ value\: [int](int.md)\ )
- [int](int.md) **get_seed**\ (\ )

Initializes the random number generator state based on the given seed value. A given seed will give a reproducible sequence of pseudo-random numbers.

\ **Note:** The RNG does not have an avalanche effect, and can output similar random streams given similar seeds. Consider using a hash function to improve your seed quality if they're sourced externally.

\ **Note:** Setting this property produces a side effect of changing the internal [state](RandomNumberGenerator.md#RandomNumberGenerator_property_state), so make sure to initialize the seed *before* modifying the [state](RandomNumberGenerator.md#RandomNumberGenerator_property_state):

\ **Note:** The default value of this property is pseudo-random, and changes when calling [randomize()](RandomNumberGenerator.md#RandomNumberGenerator_method_randomize). The ``0`` value documented here is a placeholder, and not the actual default seed.

```
    var rng = RandomNumberGenerator.new()
    rng.seed = hash("Redot")
    rng.state = 100 # Restore to some previously saved state.
```


<hr class="classref-item-separator"/>

<a id="RandomNumberGenerator_property_state"></a>

<!-- classref-property -->

[int](int.md) **state** = `0` [🔗](#RandomNumberGenerator_property_state)

<!-- classref-property-setget -->

- `void` **set_state**\ (\ value\: [int](int.md)\ )
- [int](int.md) **get_state**\ (\ )

The current state of the random number generator. Save and restore this property to restore the generator to a previous state:

```
    var rng = RandomNumberGenerator.new()
    print(rng.randf())
    var saved_state = rng.state # Store current state.
    print(rng.randf()) # Advance internal state.
    rng.state = saved_state # Restore the state.
    print(rng.randf()) # Prints the same value as previously.
```


\ **Note:** Do not set state to arbitrary values, since the random number generator requires the state to have certain qualities to behave properly. It should only be set to values that came from the state property itself. To initialize the random number generator with arbitrary input, use [seed](RandomNumberGenerator.md#RandomNumberGenerator_property_seed) instead.

\ **Note:** The default value of this property is pseudo-random, and changes when calling [randomize()](RandomNumberGenerator.md#RandomNumberGenerator_method_randomize). The ``0`` value documented here is a placeholder, and not the actual default state.

<hr class="classref-section-separator"/>

<!-- classref-descriptions-group -->

## Method Descriptions

<a id="RandomNumberGenerator_method_rand_weighted"></a>

<!-- classref-method -->

[int](int.md) **rand_weighted**\ (\ weights\: [PackedFloat32Array](PackedFloat32Array.md)\ ) [🔗](#RandomNumberGenerator_method_rand_weighted)

Returns a random index with non-uniform weights. Prints an error and returns ``-1`` if the array is empty.




```gdscript
    var rng = RandomNumberGenerator.new()

    var my_array = ["one", "two", "three", "four"]
    var weights = PackedFloat32Array([0.5, 1, 1, 2])

    # Prints one of the four elements in `my_array`.
    # It is more likely to print "four", and less likely to print "one".
    print(my_array[rng.rand_weighted(weights)])
```




<hr class="classref-item-separator"/>

<a id="RandomNumberGenerator_method_randf"></a>

<!-- classref-method -->

[float](float.md) **randf**\ (\ ) [🔗](#RandomNumberGenerator_method_randf)

Returns a pseudo-random float between ``0.0`` and ``1.0`` (inclusive).

<hr class="classref-item-separator"/>

<a id="RandomNumberGenerator_method_randf_range"></a>

<!-- classref-method -->

[float](float.md) **randf_range**\ (\ from\: [float](float.md), to\: [float](float.md)\ ) [🔗](#RandomNumberGenerator_method_randf_range)

Returns a pseudo-random float between `from` and `to` (inclusive).

<hr class="classref-item-separator"/>

<a id="RandomNumberGenerator_method_randfn"></a>

<!-- classref-method -->

[float](float.md) **randfn**\ (\ mean\: [float](float.md) = 0.0, deviation\: [float](float.md) = 1.0\ ) [🔗](#RandomNumberGenerator_method_randfn)

Returns a [normally-distributed](https://en.wikipedia.org/wiki/Normal_distribution), pseudo-random floating-point number from the specified `mean` and a standard `deviation`. This is also known as a Gaussian distribution.

\ **Note:** This method uses the [Box-Muller transform](https://en.wikipedia.org/wiki/Box%E2%80%93Muller_transform) algorithm.

<hr class="classref-item-separator"/>

<a id="RandomNumberGenerator_method_randi"></a>

<!-- classref-method -->

[int](int.md) **randi**\ (\ ) [🔗](#RandomNumberGenerator_method_randi)

Returns a pseudo-random 32-bit unsigned integer between ``0`` and ``4294967295`` (inclusive).

<hr class="classref-item-separator"/>

<a id="RandomNumberGenerator_method_randi_range"></a>

<!-- classref-method -->

[int](int.md) **randi_range**\ (\ from\: [int](int.md), to\: [int](int.md)\ ) [🔗](#RandomNumberGenerator_method_randi_range)

Returns a pseudo-random 32-bit signed integer between `from` and `to` (inclusive).

<hr class="classref-item-separator"/>

<a id="RandomNumberGenerator_method_randomize"></a>

<!-- classref-method -->

`void` **randomize**\ (\ ) [🔗](#RandomNumberGenerator_method_randomize)

Sets up a time-based seed for this **RandomNumberGenerator** instance. Unlike the [@GlobalScope](@GlobalScope.md) random number generation functions, different **RandomNumberGenerator** instances can use different seeds.

